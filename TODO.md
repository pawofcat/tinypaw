# TinyPaw 功能清单与开发计划

## 当前状态 (v0.2.0 - 稳定性增强)

### ✅ 已实现 (9 个工具)

| 类别 | 工具 | 状态 |
|------|------|------|
| 文件操作 | `read` / `write` / `edit` | ✅ |
| 系统 | `exec` | ✅ |
| 网络 | `web_search` / `web_fetch` | ✅ (简化版) |
| 记忆 | `memory_search` / `memory_get` / `memory_append` | ✅ |

### ✅ 核心架构

- Agent 循环（LLM + tool calling）
- 会话管理（**持久化存储**）✅
- 配置系统（JSON 文件）
- CLI 接口
- **TypeScript 支持** ✅ (v0.1.1)
- **错误处理与重试** ✅ (v0.2.0)
- **Token 管理** ✅ (v0.2.0)

---

## ✅ v0.2.0 已完成

### 1. 记忆持久化 ✅
**文件**: `src/session-store.ts`

**功能**:
- 会话自动保存到 `memory/sessions/<session-key>.json`
- 启动时自动加载历史会话
- 支持会话列表、切换、删除
- 元数据管理（创建时间、更新时间、消息数）

---

### 2. 错误处理与重试 ✅
**文件**: `src/retry.ts`

**功能**:
- LLM API 指数退避重试（默认 3 次）
- API 请求超时控制（60 秒）
- 工具执行超时控制（30 秒）
- 限流自动处理（429 错误等待 5 秒）
- 网络错误自动重试
- 友好的错误提示

---

### 3. Token 管理 ✅
**文件**: `src/token-manager.ts`

**功能**:
- Token 估算（英文 4 字符/token，中文 1.5 字符/token）
- 对话历史自动裁剪（保留最近消息）
- Token 使用统计
- 可配置 token 限制（默认 128000）
- 预留 token 给 response（默认 4000）

---

## 🟡 v0.3.0 计划（中优先级）

### 4. 技能系统自动加载 ❌
**问题**: skills/ 目录仅有文档，未实现自动加载。

**需要实现**:
- 扫描 `skills/*/SKILL.md`
- 解析触发条件（关键词）
- 根据用户输入匹配合适技能
- 技能优先级和 fallback

---

### 5. 多会话支持 ❌
**问题**: 当前只有单一 `default` 会话。

**需要实现**:
- 会话创建/删除/切换命令
- 会话元数据查看
- 会话导出/导入

---

### 6. 配置验证 ❌
**问题**: 配置错误（如缺少 API key）导致运行时失败。

**需要实现**:
- 配置 schema 验证
- 启动时检查必需配置
- 配置模板生成

---

### 7. 日志系统 ❌
**问题**: 仅使用 console.log，无法调试和审计。

**需要实现**:
- 结构化日志（JSON 格式）
- 日志级别（debug/info/warn/error）
- 日志文件输出
- 敏感信息脱敏

---

## 🟢 v0.4.0 计划（低优先级）

### 8. Browser 工具 ❌
**状态**: 预留但未实现

**需要实现**:
- 简单的 HTTP 客户端（带 cookie、header）
- 或集成 Playwright（重量级）
- 网页截图（可选）

---

### 9. 子任务系统 (sessions_spawn) ❌
**状态**: 预留但未实现

**需要实现**:
- 生成子 agent 处理复杂任务
- 父子任务通信
- 任务进度汇报

---

### 10. Channel 支持 ❌
**状态**: 仅 CLI

**需要实现** (选做):
- Telegram bot
- Discord bot
- Web 界面（WebSocket）

---

### 11. 工具权限控制 ❌
**问题**: exec 可执行任意命令，有安全风险。

**需要实现**:
- 命令白名单
- 危险操作确认
- 沙箱执行（可选）

---

## 📝 代码量统计

**v0.2.0 当前**:
- `agent.ts`: ~400 行
- `cli.ts`: ~100 行
- `session-store.ts`: ~150 行
- `retry.ts`: ~100 行
- `token-manager.ts`: ~120 行
- **总计**: ~870 行 TypeScript

**v0.3.0 估算**: +400 行 → ~1270 行

---

## 🚀 版本历史

### v0.2.0 (2026-03-11)
- ✅ 记忆持久化
- ✅ 错误处理与重试
- ✅ Token 管理

### v0.1.1 (2026-03-11)
- ✅ TypeScript 迁移

### v0.1.0 (2026-03-11)
- ✅ 初始版本
- ✅ 9 个基础工具
- ✅ Agent 核心循环
