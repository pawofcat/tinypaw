# TinyPaw 功能清单与开发计划

## 当前状态 (v0.1.0)

### ✅ 已实现 (9 个工具)

| 类别 | 工具 | 状态 |
|------|------|------|
| 文件操作 | `read` / `write` / `edit` | ✅ |
| 系统 | `exec` | ✅ |
| 网络 | `web_search` / `web_fetch` | ✅ (简化版) |
| 记忆 | `memory_search` / `memory_get` / `memory_append` | ✅ |

### ✅ 核心架构

- Agent 循环（LLM + tool calling）
- 会话管理（内存存储）
- 配置系统（JSON 文件）
- CLI 接口

---

## 🔴 缺失的核心功能 (按优先级)

### 高优先级 - 影响基本可用性

#### 1. 记忆持久化 ❌
**问题**: 当前会话记忆仅保存在内存中，重启后丢失。

**需要实现**:
- 会话历史自动保存到 `memory/sessions/<session-key>.json`
- 启动时自动加载历史会话
- 支持会话列表和切换

**参考 OpenClaw**: `src/config/sessions.js`

---

#### 2. 错误处理与重试机制 ❌
**问题**: LLM API 失败、工具执行失败时没有优雅处理。

**需要实现**:
- LLM API 调用重试（指数退避）
- 工具执行超时控制
- 友好的错误提示
- 失败时的降级策略

---

#### 3. Token 管理与截断 ❌
**问题**: 长对话可能导致 token 超限。

**需要实现**:
- 会话历史 token 计数
- 自动截断旧消息（保留最近 N 条或 N tokens）
- 关键信息（system prompt）保护

---

#### 4. 工具执行结果格式化 ❌
**问题**: 工具结果直接 JSON.stringify，可能包含敏感信息或过长。

**需要实现**:
- 结果长度限制（如 10KB）
- 敏感信息过滤（API keys、密码等）
- 文件路径安全检查（防止目录遍历）

---

### 中优先级 - 提升体验

#### 5. 技能系统自动加载 ❌
**问题**: skills/ 目录仅有文档，未实现自动加载。

**需要实现**:
- 扫描 `skills/*/SKILL.md`
- 解析触发条件（关键词）
- 根据用户输入匹配合适技能
- 技能优先级和 fallback

---

#### 6. 多会话支持 ❌
**问题**: 当前只有单一 `default` 会话。

**需要实现**:
- 会话创建/删除/切换
- 会话元数据（名称、创建时间、消息数）
- 会话持久化

---

#### 7. 配置验证 ❌
**问题**: 配置错误（如缺少 API key）导致运行时失败。

**需要实现**:
- 配置 schema 验证
- 启动时检查必需配置
- 配置模板生成

---

#### 8. 日志系统 ❌
**问题**: 仅使用 console.log，无法调试和审计。

**需要实现**:
- 结构化日志（JSON 格式）
- 日志级别（debug/info/warn/error）
- 日志文件输出
- 敏感信息脱敏

---

### 低优先级 - 增强功能

#### 9. Browser 工具 ❌
**状态**: 预留但未实现

**需要实现**:
- 简单的 HTTP 客户端（带 cookie、header）
- 或集成 Playwright（重量级）
- 网页截图（可选）

---

#### 10. 子任务系统 (sessions_spawn) ❌
**状态**: 预留但未实现

**需要实现**:
- 生成子 agent 处理复杂任务
- 父子任务通信
- 任务进度汇报

---

#### 11. 消息 Channel 支持 ❌
**状态**: 仅 CLI

**需要实现** (选做):
- Telegram bot
- Discord bot
- Web 界面（WebSocket）

---

#### 12. 工具权限控制 ❌
**问题**: exec 可执行任意命令，有安全风险。

**需要实现**:
- 命令白名单
- 危险操作确认
- 沙箱执行（可选）

---

## 📊 开发路线图

### v0.2.0 - 稳定性 (1-2 周)
- [ ] 记忆持久化
- [ ] 错误处理与重试
- [ ] Token 管理
- [ ] 配置验证
- [ ] 日志系统

### v0.3.0 - 功能完善 (2-3 周)
- [ ] 技能系统自动加载
- [ ] 多会话支持
- [ ] 工具结果格式化
- [ ] 单元测试

### v0.4.0 - 扩展 (可选)
- [ ] Browser 工具
- [ ] Channel 支持（Telegram/Discord）
- [ ] Web 界面

---

## 🎯 最小可行产品 (MVP) 定义

**TinyPaw v0.2.0 应满足**:

1. ✅ 能独立完成文件操作任务
2. ✅ 能搜索网络并总结信息
3. ✅ 能记住用户偏好和历史
4. ✅ 会话重启后记忆不丢失
5. ✅ API 失败时优雅降级
6. ✅ 长时间对话不崩溃

**当前差距**: 主要是记忆持久化、错误处理、token 管理。

---

## 📝 代码量估算

| 功能 | 预估代码量 | 优先级 |
|------|-----------|--------|
| 记忆持久化 | ~100 行 | 🔴 高 |
| 错误处理 | ~80 行 | 🔴 高 |
| Token 管理 | ~60 行 | 🔴 高 |
| 配置验证 | ~50 行 | 🟡 中 |
| 日志系统 | ~70 行 | 🟡 中 |
| 技能加载 | ~100 行 | 🟡 中 |
| 多会话 | ~80 行 | 🟡 中 |
| 单元测试 | ~200 行 | 🟢 低 |

**总计**: ~740 行（当前 ~400 行 → v0.2.0 约 ~1100 行）

---

## 🚀 建议下一步

**立即实现** (v0.2.0):
1. 记忆持久化（最关键）
2. 错误处理与重试
3. Token 管理

**理由**: 这三个功能直接影响基本可用性和稳定性，是 MVP 的核心。
