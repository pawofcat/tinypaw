# TinyPaw 功能清单与开发计划

## 当前状态 (v0.3.0 - 会话管理增强)

### ✅ 已实现 (9 个工具 + 会话管理)

| 类别 | 工具/功能 | 状态 |
|------|------|------|
| 文件操作 | `read` / `write` / `edit` | ✅ |
| 系统 | `exec` | ✅ |
| 网络 | `web_search` / `web_fetch` | ✅ (简化版) |
| 记忆 | `memory_search` / `memory_get` / `memory_append` | ✅ |
| 会话管理 | 完整的会话管理系统 | ✅ (v0.3.0 新增) |

### ✅ 核心架构

- Agent 循环（LLM + tool calling）
- **会话管理器（SessionManager）** ✅
  - 上下文窗口自动控制（token + 消息数双重限制）
  - 会话创建/切换/删除/重置
  - 持久化存储
  - `/` 前缀命令支持
- 配置系统（JSON 文件）
- CLI 接口（支持命令）
- TypeScript 支持 ✅
- 错误处理与重试 ✅
- Token 管理 ✅

---

## ✅ v0.3.0 已完成（本次）

### 1. 会话管理器核心 ✅
**文件**: `src/session-manager.ts` (~600 行)

**功能**:
- ✅ 会话创建 (`/new <name>`)
- ✅ 会话切换 (`/switch <name>` 或 `/sw`)
- ✅ 会话重置 (`/reset`) - 清空历史，保留 system
- ✅ 会话删除 (`/delete <name>` 或 `/del`)
- ✅ 会话列表 (`/list` 或 `/ls`)
- ✅ 会话信息 (`/info`) - 显示 token 数、消息数
- ✅ 命令帮助 (`/help` 或 `/h`)

### 2. 上下文窗口控制 ✅
**功能**:
- ✅ Token 数限制（可配置，默认 128000）
- ✅ 消息数限制（可配置，默认 100）
- ✅ 自动裁剪旧消息
- ✅ 保留 system 消息
- ✅ 保留最小消息数（可配置，默认 5）

### 3. CLI 命令解析 ✅
**文件**: `src/cli.ts` (更新)

**功能**:
- ✅ `/` 前缀命令识别
- ✅ 命令输出格式化
- ✅ 会话切换后提示更新
- ✅ 命令参数验证

### 4. Agent 集成 ✅
**文件**: `src/agent.ts` (更新)

**功能**:
- ✅ 使用 SessionManager 替代 SessionStore
- ✅ 自动获取当前会话
- ✅ 消息自动保存到会话管理器
- ✅ 兼容函数导出

### 5. 单元测试 ✅
**文件**: 
- `test/session-manager.test.ts` (~400 行)
- `test/agent.test.ts` (~300 行)

**覆盖率**:
- ✅ Token 估算测试
- ✅ 会话基本操作测试
- ✅ 会话管理命令测试
- ✅ 上下文窗口控制测试
- ✅ 命令解析测试
- ✅ 会话持久化测试
- ✅ 工具执行测试
- ✅ 配置加载测试

**测试结果**: 56 个测试，54 个通过 ✅

---

## 🟡 v0.4.0 计划（中优先级）

### 6. 技能系统自动加载 ❌
**问题**: skills/ 目录仅有文档，未实现自动加载和匹配逻辑。

**需要实现**:
- 扫描 `skills/*/SKILL.md`
- 解析触发条件（关键词）
- 根据用户输入匹配合适技能
- 技能优先级和 fallback

---

### 7. 配置验证 ❌
**问题**: 配置错误（如缺少 API key）导致运行时失败。

**需要实现**:
- 配置 schema 验证
- 启动时检查必需配置
- 配置模板生成

---

### 8. 日志系统 ❌
**问题**: 仅使用 console.log，无法调试和审计。

**需要实现**:
- 结构化日志（JSON 格式）
- 日志级别（debug/info/warn/error）
- 日志文件输出
- 敏感信息脱敏

---

## 🟢 v0.5.0 计划（低优先级）

### 9. Browser 工具 ❌
**状态**: 预留但未实现

**需要实现**:
- 简单的 HTTP 客户端（带 cookie、header）
- 或集成 Playwright（重量级）
- 网页截图（可选）

---

### 10. 子任务系统 (sessions_spawn) ❌
**状态**: 预留但未实现

**需要实现**:
- 生成子 agent 处理复杂任务
- 父子任务通信
- 任务进度汇报

---

### 11. Channel 支持 ❌
**状态**: 仅 CLI

**需要实现** (选做):
- Telegram bot
- Discord bot
- Web 界面（WebSocket）

---

### 12. 工具权限控制 ❌
**问题**: exec 可执行任意命令，有安全风险。

**需要实现**:
- 命令白名单
- 危险操作确认
- 沙箱执行（可选）

---

## 📝 代码量统计

**v0.3.0 当前**:
- `agent.ts`: ~420 行
- `cli.ts`: ~80 行
- `session-manager.ts`: ~600 行 (新增)
- `session-store.ts`: ~150 行 (已弃用，保留兼容)
- `retry.ts`: ~100 行
- `token-manager.ts`: ~120 行
- **测试文件**: ~700 行 (新增)
- **总计**: ~2170 行 TypeScript

**v0.4.0 估算**: +400 行 → ~2570 行

---

## 🚀 版本历史

### v0.3.0 (2026-03-24) - 会话管理增强
- ✅ 完整的会话管理器（SessionManager）
- ✅ `/` 前缀命令支持
- ✅ 上下文窗口自动控制
- ✅ 单元测试（56 个测试）

### v0.2.0 (2026-03-11)
- ✅ 记忆持久化
- ✅ 错误处理与重试
- ✅ Token 管理

### v0.1.1 (2026-03-11)
- ✅ TypeScript 迁移

### v0.1.0 (2026-03-11)
- ✅ 初始版本
- ✅ 9 个基础工具
- ✅ Agent 核心循环

---

## 📋 命令参考

### 会话管理命令

| 命令 | 简写 | 描述 | 示例 |
|------|------|------|------|
| `/new <name>` | - | 创建新会话并切换 | `/new project-a` |
| `/switch <name>` | `/sw` | 切换到已有会话 | `/switch default` |
| `/reset` | - | 重置当前会话 | `/reset` |
| `/delete <name>` | `/del` | 删除会话 | `/del old-session` |
| `/list` | `/ls` | 列出所有会话 | `/ls` |
| `/info` | - | 显示当前会话信息 | `/info` |
| `/help` | `/h` | 显示帮助 | `/h` |

### 会话信息示例

```
📊 当前会话：default
   消息数：15
   Token 数：~2340
   创建时间：2026/3/24 22:30:00
   最后更新：刚刚
```
